import { theme } from '../../utils/theme';

export const createStyles = () => {
	return {
		main: {
			mt: theme.spacing(4),
			display: 'flex',
			flexDirection: 'column',
			alignItems: 'center'
		}
	};
};
